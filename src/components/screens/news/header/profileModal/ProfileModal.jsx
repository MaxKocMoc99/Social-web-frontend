import ProfileModalTheme from "./ProfileModalTheme"
const ProfileModal = ({profileModal, setProfileModal}) => {
  return (
      <div className={profileModal}>
          <ProfileModalTheme/>
    </div>
  )
}

export default ProfileModal